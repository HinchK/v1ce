# Base44 source archive

This directory preserves the source export that was provided for the original V1CE app.

- `V1CE_Code.txt` is the archived source export.
- The working Expo implementation lives at the repository root.
- Keep this archive unchanged; build changes belong in the working source tree.
