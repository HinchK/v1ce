# Base44 source archive

This directory preserves source artifacts retrievable from the V1CE conversation/library.

## Archived source

- `V1CE_17ab.pdf` — original source PDF supplied by the project owner; preserved byte-for-byte.
- `V1CE_daea.txt` — text/chat export of the same Base44 source inventory for searchable reference.
- `V1CE_Code.txt` — existing source archive already present in the repository; verified during this pass.
- `Pasted code.js`
- `Pasted code(1).js`
- `Pasted code(2).js`
- `Pasted code(3).js`
- `Pasted code(4).js`
- `Pasted code(5).js`
- `Pasted code(6).js`

## Verification

The seven newly archived pasted-code files were fetched back from GitHub and compared against their saved source after normalizing only CRLF/LF line endings. All seven matched.

`V1CE_Code.txt` was already present and was left unchanged.

This archive intentionally does not rewrite, redesign, or clean up the original source. Build changes belong in the working source tree.
